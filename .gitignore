# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
.env.local
.sentryclirc
undefined:/Users/

# Windows download metadata (can appear in WSL)
*:Zone.Identifier

# AI/agent scaffolding (local-only, not shared to GitHub)
/brain/
/CLAUDE.md
/AGENTS.md
/PLAN1.md
/Plan2.md
/.verify/
/.marker

# Dev tooling artifacts (local-only)
/.playwright-mcp/

# Generated invoice PDFs (output of docs/invoices/generate_*.py; not source assets)
docs/invoices/*.pdf

# Client admin: registrar, hosting, billing and messaging drafts (local-only).
# This repo is PUBLIC and Phil only receives the website at handover, so none of
# this belongs in it. Keep working copies here; they are never committed.
docs/afrihost-to-cloudflare-migration.md
docs/handoff-pcquanti-migration.md
docs/xneelo-support-nameserver-500.md
docs/email-to-xneelo-support-*.md
docs/whatsapp-to-phil-*.md
docs/za-registrar-comparison.md
docs/email-log-phil.md
docs/scripts/cloudflare-*.sh
